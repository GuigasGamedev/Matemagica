direction = random(359);

image_xscale = random_range(.5, 1);
image_yscale = image_xscale;

escalaUp = random_range(.5, .7);

speed = random_range(30, 50);

speedDecay = random_range(.5, 1);

image_alpha = 1;

alphaDecay = random_range(.03, .08);
